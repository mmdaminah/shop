const initialState = {
    isLogin:"",
    accessToken:"",
    refreshToken:"",
    role:""
}
const UserReducer = (state = initialState,action:any)=>{
    switch(action.type) {
        default:
            return state
    }
}
export default UserReducer;